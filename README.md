Small application to demonstrate how values can be put into and retrieved from a local Redis cache. \
This command can be used to set up the local Redis cache in Docker: \
docker run --name redis-local -p 6379:6379 -d redis
